export enum StatusEnum {
  DISABLE = 0,
  ENABLE = 1
}

export enum TaskTypeEnum {
  CRON = 0,
  INTERVAL = 1
}

export enum ProviderEnum {
  SMS = 'sms',
  EMAIL = 'email',
  CAPTCHA = 'captcha'
}

export enum MenuTypeEnum {
  DIRECTORY = 0,
  // 菜单
  MENU = 1,
  // 权限
  PERMISSION = 2
}
